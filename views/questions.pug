extends layout.pug

include utils.pug

block content
  div
    h2 Questions
    div
      form(method='get' action='/questions/ask')
        button(type='submit' class='default-ask-button') Ask Question
    ul
      each question in questions
        li
          a(value=question.id href="/questions/" + question.id)= question.title
